services:
  database:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: books
      POSTGRES_HOST_AUTH_METHOD: trust
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d books"]
      interval: 2s
      timeout: 2s
      retries: 20
    volumes:
      - example-data:/var/lib/postgresql/data

  migrate:
    build:
      context: ../..
      dockerfile: examples/split-wsgi-asgi/Dockerfile
    command:
      - sh
      - -c
      - python manage.py migrate --noinput && python manage.py seed_books
    environment: &app_environment
      DATABASE_HOST: database
      DATABASE_NAME: books
      DATABASE_USER: postgres
      DJANGO_SECRET_KEY: local-example-only-change-in-production
    depends_on:
      database:
        condition: service_healthy

  wsgi:
    build:
      context: ../..
      dockerfile: examples/split-wsgi-asgi/Dockerfile
    command:
      - gunicorn
      - acme.wsgi:application
      - --bind
      - 0.0.0.0:8000
      - --workers
      - "2"
    environment: *app_environment
    depends_on:
      migrate:
        condition: service_completed_successfully

  asgi:
    build:
      context: ../..
      dockerfile: examples/split-wsgi-asgi/Dockerfile
    command:
      - uvicorn
      - acme.asgi:application
      - --host
      - 0.0.0.0
      - --port
      - "8001"
      - --workers
      - "2"
    environment: *app_environment
    depends_on:
      migrate:
        condition: service_completed_successfully

  nginx:
    image: nginx:1-alpine
    depends_on:
      - wsgi
      - asgi
    ports:
      - "${EXAMPLE_HTTP_PORT:-8080}:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro

volumes:
  example-data:
